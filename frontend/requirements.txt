# Open Dashboard dev server (serves `frontend/` static files + /api/*)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# IFC upload + parse (dashboard_server.py imports backend/ifcprocesser)
python-multipart
ifcopenshell

# Public HTTPS URL for demos (python frontend/serve_public.py)
pyngrok>=7.2.0
